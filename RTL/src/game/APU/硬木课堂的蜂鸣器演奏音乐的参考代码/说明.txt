网址：https://www.yuque.com/yingmuketang/01/shixeo
只有音调（周期）、节拍（音乐时长的设置）；
没有音量的设置（PWM实现的时候不方便）；
只有占空比50%的矩形波这一种音色，可以进行修改；
if (cnt == (pwm_parameter>>1)) begin 此处就是表示占空比50%的方波
                    clk_pwm <= ~clk_pwm;

如果修改为
if (cnt <= (pwm_parameter>>2))  此处就是表示占空比75%或者25%的方波
                    clk_pwm <= ~clk_pwm;